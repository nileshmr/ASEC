const BASE_URL = "http://localhost:5000"; // Last me slash mat lagao

export const API = {
  BASE_URL,
  REGISTER: `${BASE_URL}/api/register`,
  NEWSLETTER: `${BASE_URL}/api/newsletter`,
  CONTACT: `${BASE_URL}/api/contact`, // Added contact endpoint
  // Add other endpoints here as needed
};

export default API;
